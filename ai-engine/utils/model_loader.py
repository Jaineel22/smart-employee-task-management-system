from typing import Any, Optional


def load_model(path: Optional[str] = None) -> Any:
    """Stub model loader. In a real setup this would load a persisted model from disk.
    Returns None for now.
    """
    return None
