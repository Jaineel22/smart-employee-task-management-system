spring.application.name=smart-task-management

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smart_task_management
spring.datasource.username=root
spring.datasource.password=root123

# JPA / Hibernate
    
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=8080

# JWT Configuration
jwt.secret=smartTaskManagementSecretKeyForNcodeSolutionsProject2026
jwt.expiration=86400000

ai.engine.url=http://localhost:8000
